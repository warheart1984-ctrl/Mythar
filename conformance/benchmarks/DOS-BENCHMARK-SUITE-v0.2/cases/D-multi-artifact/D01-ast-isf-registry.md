# D01 — AST, ISF, and Registry Decision

**Status:** Draft — Specified

Require a single decision graph to bind AST, ISF, and registry evidence. Record graph depth, branching factor, invariant density, and replay result. No result is recorded yet.
