# Traceability Harness

A pass requires the complete path Input → AST → ISF → typed DOS nodes → invariant outcomes → final decision state. Each transition must carry an evidence reference or an explicit reason it has none.

