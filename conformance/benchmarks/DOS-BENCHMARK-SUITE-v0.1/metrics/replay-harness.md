# Replay Harness

Canonicalize a decision graph as UTF-8 JSON with sorted keys, replay it through the same declared adapter, and compare node count, edge count, invariant outcomes, and SHA-256 graph hash. Cross-runner replay requires separately recorded machine/runtime evidence.

