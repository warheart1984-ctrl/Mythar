# Specification Registry Conformance Mirror

This page mirrors the conformance artifact for the queryable specification registry endpoint.

## Artifact
- [Specification Registry Conformance JSON](../../../constitutional/specification-registry.conformance.json)

## Current Status
- Documentation-first endpoint discovery surface
- Read-only registry query endpoint
- Mirrors the checked-in registry artifact

## Conformance Notes
- CIS Core remains the constitutional authority.
- The registry is queryable but not mutable through this endpoint.
- The endpoint mirrors the registry rather than redefining it.

